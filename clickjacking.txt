A web user accesses a decoy website (perhaps this is a link provided by an email) and clicks on a button to win a prize. 
Unknowingly, they have been deceived by an attacker into pressing an alternative hidden button and this results in the payment of an account on another site. 
This is an example of a clickjacking attack.

Clickjacking attacks use CSS to create and manipulate layers.


VVI TIP:- Agar website m "CSP" ya "xframe options" aur "frame busting" na ho to clickjacking ka attack ho skta h.


#1 Lab:-  Basic clickjacking with CSRF token protection

      1. Go to exploit server of lab and add target url in iframe
      2. Add some css to hide iframe
      3. Match the button position of target website with "delete" button

-------------------------------------------------------------------------------

NOTE:-  For clickjacking , use Burp's Clickbandit tool.

#2 Lab:- Clickjacking with form input data prefilled from a URL parameter.

       1. Go to exploit server of lab and add target url in iframe and append my-account?email=hacker@attacker-website.com
       2. Add some css to hide iframe
       3.Done

-------------------------------------------------------------------------------

#3 Lab:- Clickjacking with a frame buster script

      1. Go to exploit server of lab and add target url in iframe add <iframe sandbox ="allow-forms" just above that line


      TIP:- Frame buster script prevent from loading website in iframe but if we use sandbox="allow-forms" then it will allow to load website in iframe.

------------------------------------------------------------------------------- 

#4 Lab:- Exploiting clickjacking vulnerability to trigger DOM-based XSS

      1.append at last line <iframe src ="https://.web-security-academy.net/feedback?name=<img src=1 onerror=print()>&email=hacker@attacker-website.com&subject=test&message=test#feedbackResult"></iframe>